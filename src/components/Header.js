export default function Header(props) {
  return (
    <div className="header">
      <h1>Memory Card</h1>
      <p>
        Try to click all the cards without clicking the same one twice. Every 12
        clicks the board resets and you click all of them again.
      </p>
    </div>
  );
}
